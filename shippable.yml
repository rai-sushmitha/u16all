language: none
services:
 - postgres
build:
 pre_ci_boot:
  image_name: sushmitharai/latest16
  image_tag: latest
  pull: false
 ci:
  - sudo su postgres -c "psql -c '\l'"
  - shippable_service postgres stop
